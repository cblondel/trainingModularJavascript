var Polygon_Module;

var Shape_Module = (function(mainModule, subModule) {

    var Polygon = mainModule.Polygon = mainModule.Polygon || subModule;

    return mainModule;

})(Shape_Module || {}, Polygon_Module || {});
